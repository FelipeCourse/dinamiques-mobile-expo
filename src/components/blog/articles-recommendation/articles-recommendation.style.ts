import { StyleSheet } from 'react-native';

export const s = StyleSheet.create({
  articlesRecommendationContainer: {
    marginTop: 40,
  },
  articlesRecommendationItem: {
    marginBottom: 40,
  },
});
